{
  "name": "aimeeting",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend,asr -c green,cyan,yellow \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"python3 backend/asr_server.py\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:asr": "python3 backend/asr_server.py",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "build:client": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}